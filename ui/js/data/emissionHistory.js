// emission_history.js is generated by src/importGlobalCarbonProject.jl and loaded as a
// classic script, so it publishes its data as globals. Adapt it to a module here rather
// than changing the generator.
//
// Regions Global, Non-OECD and ROW are derived, and everything is converted from GtC to
// GtCO2, by completeExternalData() in sspData.js.
export const CO2emissionHistory = window.CO2emissionhistory;
export const backgroundDataStart = window.backgrounddatastart;
